#include "../pforth.h"
#include "../pf_guts.h"

/**
 * Process a XT that represents a word in the dictionary.
 * If that word is a raylib word, process it.
 */
void ProcessRaylibWord() {
  // switch (Token) {
  //   case ID_INIT_WINDOW: { /* ( +n +n c-addr u --  ) */
  //     cell_t width = M_POP;
  //     cell_t height = M_POP;
  //     char *title = (char *)M_POP;
  //     cell_t len = TOS;
  //     printf("\nInitWindow( %d, %d, \"%s\" )", width, height, "WIP");
  //     break;
  // }
}